# 悦拜AI工具平台前端

React + Vite + Ant Design 前端工程。

本地开发：

```bash
npm install
npm run dev
```

默认访问：

```text
http://127.0.0.1:8790
```

开发环境会把 `/api`、`/flowbot`、`/install` 代理到后台：

```text
http://127.0.0.1:8788
```

# douyin-downloader Claude Guidance

Read `AGENTS.md` for the full project rules.

## Shared Logic

- This project extends shared Python backend logic from `../content-assets-shared`.
- When fixing shared logic in `auth/`, `cli/`, `config/`, `control/`, `core/`, `storage/`, `tools/`, `utils/`, or shared tests, prefer changing `content-assets-shared` first.
